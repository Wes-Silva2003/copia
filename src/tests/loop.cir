func main() {
    x = 0;
    while x < 5 {
        if x % 2 == 0 {
            print("Número par: " + x);
        } else {
            print("Número ímpar: " + x);
        }
        x = x + 1;
    }
}